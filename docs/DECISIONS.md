# Decisions — Personal Site

> Approved เท่านั้น · ที่มา: `docs/DEBATE.md` (Brand / UX / Devil · 2 รอบ) · 2026-09-25  
> ถ้าขัดกับหัวข้อ `## Brainstorm` ใน `docs/PROFILE.md` ให้ถือไฟล์นี้เป็นหลัก

## สรุปการโต้วาที

ทั้งสามบทบาทเห็นตรงกันว่า HR/ผู้จ้างงานคือกลุ่มหลัก และหน้าแรกควรสั้น มีแค่ headline, tagline และ CTA เดียว ส่วนเรื่องเล่าให้ไปอยู่ที่ About จุดที่ขัดกันมากที่สุดมีสามเรื่อง:

- **ความยาว headline:** Brand อยากเติม "ดูแลต่อได้ยาว" แต่ UX ค้านเพราะยาวเกินบนมือถือ
- **Tagline:** Devil จับได้ว่าข้อความชุดใหม่เคลม Laravel 5 ปีเกินจริง
- **การแสดงอีเมลและฟอร์มที่ยังตอบ 501:** UX อยากให้ใช้ง่าย ส่วน Devil ห่วงเรื่อง privacy และ spam

ข้อสรุปคือ:

- ตัดคำที่เคลมเกินจริงออก
- เก็บ AI agents ไว้เป็นบรรทัดรองในน้ำเสียงที่ชัดว่าเจ้าของเป็นคนคุมงาน
- เลื่อน Guestbook ไป Later
- Contact ต้องไม่ทำข้อความของผู้ใช้หาย และไม่โชว์ที่อยู่อีเมลตรง ๆ

## การตัดสินใจ (ตาราง)

| ID | หัวข้อ | ตัดสินใจ | เหตุผลสั้น | ใครเสนอ (Brand/UX/Devil) |
|----|--------|----------|------------|---------------------------|
| D1 | Headline | **คงเดิม** "PHP Developer ที่เปลี่ยนโจทย์ธุรกิจให้เป็นเว็บที่ใช้งานได้จริง" · ไม่เติม "และดูแลต่อได้ยาว" แต่ใช้วลีนี้ใน Bio ย่อหน้าแรก (มีอยู่แล้ว) หรือเป็นหัวข้อย่อยของ About | จอ 360px จะยาว 2–3 บรรทัด · วลีนี้ยังได้ใช้สร้างความต่าง | UX (ชนะ Brand) |
| D2 | Tagline + โทน Bio | Tagline → "5+ ปีกับ PHP · เว็บองค์กรและระบบหลังบ้าน · กำลังต่อยอด Laravel · ทำงานร่วมกับ AI agents" (ตัด "Full-stack") · Bio ย่อหน้า 3 เปลี่ยนเป็นน้ำเสียง "ผมออกแบบและตรวจทุกส่วนเอง" · Bio ย่อหน้า 4 เป็น CTA ที่บอกว่าอยากคุยเรื่องอะไร **แก้ใน PROFILE แล้ว** | "ทำอยู่" แทน "กำลังไป" แต่ไม่เคลม Laravel 5 ปี · ปิด R4/R5 | Brand + Devil |
| D3 | Audience | กลุ่มหลักคือ HR/ผู้จ้างงาน · กลุ่มรองคือลูกค้า · เพื่อน Dev และมือใหม่**ไม่เขียนถึงบน Home** (เก็บไว้ทำ Blog ใน Later) | ข้อความที่พยายามคุยกับทุกกลุ่มจะไม่โดนใจใครเลย (R6) | Brand + Devil |
| D4 | IA + Home | เมนู 4 รายการ: `หน้าแรก · เกี่ยวกับผม · ทักษะและความสนใจ · ติดต่อ` · Home = headline + tagline + CTA หลัก `ติดต่อผม` (ไปหน้า Contact) + CTA รอง `ดู GitHub` เหนือ fold · Bio เต็มแสดงที่ About · AI agents ไม่เป็นข้อความเด่นของ Hero | HR สแกนบนมือถือ 10 วินาที · ลดความซ้ำระหว่าง About กับ Interests | UX |
| D5 | ช่องทางติดต่อ | อีเมลแสดงเป็นลิงก์ `mailto` ที่ตัวลิงก์เขียนว่า "ส่งอีเมล" **ไม่พิมพ์ที่อยู่ลงใน markup เป็นข้อความ** · ช่องที่ค่าเป็น `—` หรือว่าง (LinkedIn) **ไม่ render** · จะเปลี่ยนเป็น alias หรืออีเมลงานไหม = เจ้าของตัดสิน (L4) | ลดการ scrape แต่ยังมีช่องทางที่ใช้ได้แน่นอน · ขีดลอย ๆ ทำให้เว็บดูไม่เสร็จ | UX + Devil (R1) |
| D6 | ฟอร์ม Contact ขณะ API ยังไม่พร้อม | หน้า Contact มีลิงก์ "ส่งอีเมล" ให้เห็นเสมอ · ฟอร์มแสดงได้ · ถ้าได้ 501 หรือ error **ไม่ล้างข้อความในช่อง** และแสดงข้อความสั้นว่า "ระบบส่งข้อความยังไม่พร้อม — ส่งอีเมลหาผมได้เลย ข้อความของคุณยังอยู่ในช่อง" · ไม่แสดง stack trace · ตัด "501 ต้องแสดงข้อความสวย" ออกจาก Must ในฐานะงานออกแบบ | CTA ไม่ตาย (R7 ปิด) · ทำได้ฝั่ง frontend โดยไม่ต้องแตะ `db.ts` | UX + Brand (Devil ยอมรับรอบ 2) |
| D7 | Interests | แสดง**ครบทุก bullet** (ต้องแก้ L2 ก่อน) · รูปแบบการ์ด = ชื่อ + บรรทัดเดียวว่า "ทำอะไรกับมัน" · เนื้อหาบรรทัดนี้ให้เจ้าของเติมใน PROFILE ส่วน Claude ไม่เดาแทน (ระหว่างนี้แสดงชื่อเพียงอย่างเดียว) | การ์ดที่มีแค่ป้ายคำดูว่าง · ถ้าเห็นแค่การ์ดเดียวจะดูเหมือนเว็บพัง | UX |
| D8 | About + parser | About ไม่บังคับครบ 4 ย่อหน้า แต่ต้องแสดง**ทุกย่อหน้าที่อยู่ใน PROFILE** · FALLBACK ของ `loadProfile()` **ห้าม**ออกสู่สาธารณะเมื่อ PROFILE มีเนื้อหา · ต้องแก้ L2 ก่อนงาน UI อื่น | R8 · เรื่องเล่าย้ายมาที่ About จึงต้องแสดงหลายย่อหน้า | Devil + UX |
| D9 | Guestbook | **เลื่อนไป Later** ไม่อยู่ในเมนูและ footer ของ v1 · จะเปิดได้เมื่อครบทุกข้อ: escape output · rate limit · honeypot · เพดานความยาว · moderation/อนุมัติก่อนแสดง · เพดานจำนวนแถว | ไม่ช่วย HR และเสี่ยง spam หรือข้อความหมิ่นประมาทบนเว็บที่ใช้ชื่อจริง (R2) | Devil (Brand + UX รับ) |
| D10 | Skills | เพิ่ม `## Skills` ใน PROFILE (**แยกจาก Interests**) · ใส่เฉพาะสิ่งที่ยืนยันได้ · ถ้ายังไม่มี section นี้ หน้า "ทักษะและความสนใจ" แสดงแค่ Interests และไม่ render หัวข้อว่าง · เจ้าของเป็นคนเติม (L3) | HR มองว่า Skills กับ Interests ต่างกัน · ห้ามเคลมเกินจริง | UX + Devil |
| D11 | เงื่อนไขก่อนเปิดฟอร์มจริง (backend) | ใต้ฟอร์มมี microcopy "ใช้ข้อมูลนี้เพื่อตอบกลับคุณเท่านั้น" (frontend) · ฝั่ง backend: `DATA_DIR` บน volume ถาวร · แจ้งเตือนเจ้าของเมื่อมีข้อความใหม่ · **ไม่มี** `GET /api/contact` · กำหนดระยะเวลาเก็บและวิธีลบ · validate ความยาว | PDPA (R3) · ข้อความที่ส่งแล้วไม่มีใครเห็นก็เท่ากับ CTA ตาย | Devil + UX |
| D12 | AI agents | เก็บไว้ใน tagline (ท้ายบรรทัด) และใน Bio ในน้ำเสียงที่ชัดว่าเจ้าของคุมงาน · **ห้าม**อธิบายว่าเว็บสร้างมาอย่างไรในระดับที่พาดพิงถึงคอร์สหรือแล็บ | เป็นจุดต่างข้อเดียว แต่ต้องไม่ทำลาย credibility (R5) | Brand (UX ประนีประนอม) |
| D13 | สถานะ "เปิดรับงาน" | **ไม่ประกาศ**บนเว็บจนกว่าเจ้าของจะยืนยันเอง (L6) · CTA ใช้ถ้อยคำกลาง ๆ ไปก่อน | อาจกระทบงานปัจจุบัน (R9) | Devil |
| D14 | หลักฐานงาน (case study) | ขอ **case study 1 ชิ้นแบบไม่ระบุชื่อลูกค้า** (ปัญหา → สิ่งที่ทำ → ผลลัพธ์) เป็น Must ของ v1.1 · **ไม่บล็อก Lab 04** · เจ้าของเป็นคนให้เนื้อหา (L7) | ปิด R4 ได้ทางเดียว · ทั้งสามบทบาทเห็นตรงกัน | Devil + Brand + UX |

### แก้ PROFILE ในรอบนี้ (D2)

- `## Tagline`: "5+ ปีกับเว็บองค์กรและระบบหลังบ้าน · ขยายสู่ Laravel & Full-stack · ทำงานร่วมกับ AI agents" → "5+ ปีกับ PHP · เว็บองค์กรและระบบหลังบ้าน · กำลังต่อยอด Laravel · ทำงานร่วมกับ AI agents"
- `## Bio` ย่อหน้า 3: "…เว็บนี้ก็สร้างด้วยวิธีนั้นเหมือนกัน" → "…เว็บนี้ผมออกแบบและตรวจทุกส่วนเอง โดยมี AI agents เป็นผู้ช่วยในการพัฒนา"
- `## Bio` ย่อหน้า 4 (CTA): เปลี่ยนเป็นข้อความที่บอกเรื่องที่อยากคุย โดยไม่ประกาศสถานะเปิดรับงาน (D13)
- ไม่แก้ Headline (D1) · ไม่แตะ `## Brainstorm` (ไฟล์นี้ override แทน)

## สิ่งที่เลื่อนออก (Out of scope v1)

- Guestbook (D9) รวมถึงลิงก์ใน footer
- Portfolio หลายชิ้น / Case study เต็มรูปแบบ (v1 ขอแค่ 1 ชิ้น ใน v1.1 · D14)
- Blog สำหรับมือใหม่ PHP (กลุ่มมือใหม่ · D3)
- เวอร์ชันภาษาอังกฤษ / i18n · SEO / OG image
- Login / CMS / หน้าแอดมินดูข้อความ
- การประกาศสถานะเปิดรับงาน (รอเจ้าของ · D13)
- "หน้า 501 สวย ๆ" ในฐานะงานออกแบบแยก (แทนด้วยข้อความสั้นใน D6)

## เกณฑ์พร้อม Frontend (Lab 04)

1. **L2 แก้แล้ว:** About แสดงครบทุกย่อหน้าของ Bio และ Interests แสดงครบ 4 bullet · ไม่มี FALLBACK หลุดเมื่อ PROFILE มีเนื้อหา (D7, D8)
2. **Home ตาม D4:** headline (D1) + tagline (D2) + CTA `ติดต่อผม` + `ดู GitHub` เหนือ fold บนจอ 360px · เมนู 4 รายการ ไม่มี Guestbook (D9)
3. **Contact ตาม D5 + D6:** ลิงก์ "ส่งอีเมล" ให้เห็นเสมอ ไม่มีที่อยู่อีเมลเป็นข้อความใน markup · ช่อง `—` ไม่ render · เมื่อได้ 501 ข้อความยังอยู่ในช่อง และมีข้อความสั้นพร้อมทางไปอีเมล · มี microcopy privacy (D11)
4. **ไม่มีหัวข้อหรือช่องว่างเปล่า:** ถ้ายังไม่มี Skills หรือบรรทัดอธิบาย Interests ก็ไม่ render ส่วนนั้น (D7, D10)
5. **`npm test` เขียว** (รวม `tests/public-site.test.ts` คือไม่มีคำว่าคอร์สหรือแล็บใน markup) · ไม่แตะ `src/lib/db.ts` / `src/pages/api/**`

## GitHub issues (Lab 03)

| Issue # | Title | มาจาก Decision |
|---|---|---|
| [#1](https://github.com/supunkangolf/ai-multi-web/issues/1) | [D1][D4] Home hero + IA เมนู 4 รายการ | D1, D4 (+D9 ไม่มี Guestbook) |
| [#2](https://github.com/supunkangolf/ai-multi-web/issues/2) | [D2] Tagline + โทน Bio — ยืนยันการ render ตาม PROFILE ที่แก้แล้ว | D2 (+D12, D13) |
| [#3](https://github.com/supunkangolf/ai-multi-web/issues/3) | [D3] Audience — HR หลัก · ลูกค้ารอง · ไม่เขียนถึง Dev/มือใหม่บน Home | D3 |
| [#4](https://github.com/supunkangolf/ai-multi-web/issues/4) | [D5] ช่องทางติดต่อ — mailto "ส่งอีเมล" · ไม่ render ช่องว่าง | D5 |
| [#5](https://github.com/supunkangolf/ai-multi-web/issues/5) | [D6] ฟอร์ม Contact ขณะ API ตอบ 501 — ไม่ล้างข้อความ + ทางไปอีเมล | D6 (+D11 microcopy) |

## Lab 03 — MCP vs gh

- **ความเร็ว:** MCP สร้างได้หลาย issue ในรอบเดียว (#1–#5 ส่งพร้อมกัน) และ agent ร่าง body ให้จาก DECISIONS ได้เลย · `gh` ต้องพิมพ์หรือเตรียม body ทีละอัน แต่เร็วกว่าถ้ามีอันเดียวและรู้ว่าจะเขียนอะไร เพราะไม่ต้องผ่าน agent
- **สิทธิ์:** MCP ใช้ PAT ที่ตั้งไว้ใน config ของ MCP server (สิทธิ์ตาม scope ของ token นั้น) · `gh` ใช้ token จาก `gh auth login` ของเครื่อง · เป็นคนละ credential กัน ถ้าตัวหนึ่งใช้ได้ ไม่ได้แปลว่าอีกตัวจะใช้ได้ด้วย · เก็บ token นอก repo เสมอ
- **Audit trail:** บน GitHub ทั้งสองแบบบันทึกผู้สร้างเป็น user เดียวกัน (`supunkangolf`) · ฝั่งเครื่อง MCP มี tool call + URL ที่ได้กลับมาอยู่ในทรานสคริปต์ของ agent · `gh` มีเฉพาะ shell history · ทางที่ตรวจย้อนได้ดีที่สุดคือตาราง issue ↔ D-id ในไฟล์นี้
- **ข้อผิดพลาดที่เจอ:** MCP ไม่เจอ 401 (`get_me` ผ่านก่อนสร้าง) · label `docs` ไม่มีใน repo เลยใช้ `documentation` แทน · GitHub ไม่มีสถานะ "draft" สำหรับ issue ต้องใช้ `gh issue create --web` เพื่อตรวจก่อนกดสร้างเอง · ฝั่ง `gh` ให้บันทึกผลเพิ่มหลังรันจริง
- **เมื่อไหร่ใช้อะไร:** MCP ใช้กับงานหลายชิ้นที่ต้องแปลงจากเอกสาร ให้ agent ร่างและตรวจของที่มีอยู่ก่อน (search/list กัน issue ซ้ำ) · `gh` ใช้กับงานเดี่ยวที่คนต้องการคุมเอง, ใช้ใน script/CI หรือเมื่อ MCP ใช้ไม่ได้ (401 หรือ server ไม่ขึ้น) · MCP ใช้กับงานผลิตเท่านั้น ไม่ใช่ท่อระหว่าง Claude กับ OpenCode
